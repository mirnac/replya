# No datasource is configured yet (Postgres starter is on the classpath but unused),
# so skip its auto-configuration to let the context load in tests.
spring.autoconfigure.exclude=org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration

# Dummy WhatsApp config so property binding has values in tests.
whatsapp.api.access-token=test-token
