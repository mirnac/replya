# El datasource lo provee Testcontainers (@ServiceConnection en ReplyaApplicationTests):
# Flyway corre las migraciones contra un Postgres real y los repos JPA quedan disponibles.

# Config dummy para que el binding de propiedades tenga valores en los tests
# (no se hacen llamadas reales a WhatsApp/Anthropic desde el contextLoads).
whatsapp.api.access-token=test-token
anthropic.api-key=test-key
